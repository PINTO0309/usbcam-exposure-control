{
  "name": "usbcam-whitebalance",
  "version": "1.0.0",
  "description": "USB Camera White Balance Control Verification App",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && cp src/index.html dist/",
    "start": "tsc && cp src/index.html dist/ && electron .",
    "dev": "tsc && cp src/index.html dist/ && electron . --enable-logging"
  },
  "dependencies": {
    "electron": "^28.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
